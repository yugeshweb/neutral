"""Deterministic feature extractors, dispatched to by name from
`SourceSpec.representation.extractor` (see `..representation`).

Everything under this package is a REFERENCE / PLACEHOLDER implementation
- see the module docstrings in `reference_signal.py` and
`reference_imaging.py` for why, and how to replace them with the team's
real feature-extraction code without touching any pipeline code."""
